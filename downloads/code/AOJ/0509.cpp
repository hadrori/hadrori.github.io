#include <bits/stdc++.h>

#define int long long

using namespace std;

int n, r;
bool field[10010][10010]
signed main(){
    while(cin >> n >> r, n | r){
        memset(field, 0, sizeof(field));
        for(int i = 0; i < n; i++){
            
        }
    }
    return 0;
}
